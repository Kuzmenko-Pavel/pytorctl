from setuptools import setup, find_packages

setup(
    name="torctl",
    version="0.2",
    description="A Python library for Tor controllers",
    license="BSD",
    python_requires='>=3.5',
    packages=find_packages(),
    install_requires=[]
)
